// Install simple json-server, provides a very simple way to set up a web server that supports a full-fledged REST API server:

npm install json-server -g

// To start the json server:

json-server --watch db.json 

// Thise server serves the json object to the application/ resturant website.
// file is db.json

